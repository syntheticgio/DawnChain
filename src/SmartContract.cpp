#include "SmartContract.h"

// SmartContract is a pure-abstract base class; all behaviour is provided by
// derived classes.  This translation unit exists so the Makefile wildcard picks
// up the header and the linker has a home for the virtual destructor.
